# Scan Modal Polish

- **UI**: Rebuilt the Scan modal layout (HTML + CSS) to align controls, add spacing, and improve readability.
- **UX**: The Scan modal now restores the last-used options (hidden files, incremental mode, ignore globs) via `localStorage`.
- **Bugfix**: Removed stale references to a non-existent `run-scan` toggle when opening the Run modal.
