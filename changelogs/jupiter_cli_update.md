# Changelog – CLI Update & Versioning

- Added `--version` flag to `jupiter` CLI.
- Added `update` command to `jupiter` CLI:
  - Supports updating from a local ZIP file.
  - Supports simulated Git update.
  - Includes force option.
