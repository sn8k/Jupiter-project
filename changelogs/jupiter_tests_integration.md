# Changelog – Tests & CI

- Added `tests/test_integration.py` covering:
  - API endpoints: `/health`, `/scan`, `/analyze`, `/run`, `/meeting/status`.
  - Dynamic analysis workflow.
- Added `.github/workflows/ci.yml` for automated testing and linting on push/PR.
